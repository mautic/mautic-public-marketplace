# yaml-language-server: $schema=../vendor/symfony/routing/Loader/schema/routing.schema.json

# This file is the entry point to configure the routes of your app.
# Methods with the #[Route] attribute are automatically imported.
# See also https://symfony.com/doc/current/routing.html

# To list all registered routes, run the following command:
#   bin/console debug:router

marketplace_homepage:
    path: /
    controller: App\Controller\MarketplaceController::homepage

marketplace_index:
    path: /browse
    controller: App\Controller\MarketplaceController::index

marketplace_detail:
    path: /package/{package}
    controller: App\Controller\MarketplaceController::detail
    requirements:
        package: .+

profile_page:
    path: /profile
    controller: App\Controller\ProfileController::index

profile_api:
    path: /api/profile
    controller: App\Controller\Api\ProfileApiController::profile
    methods: [GET]

review_api_submit:
    path: /api/review/{package}
    controller: App\Controller\Api\ReviewApiController::submit
    methods: [POST]
    requirements:
        package: .+
